# CLAUDE.md

Everything about working in this repository lives in one place, so that it cannot drift into two:

@AGENTS.md

Before anything else: **never run `dev_drive.ps1`.** It repartitions disks and formats volumes.
